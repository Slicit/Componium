# The rigs this installation can use, and .chosen says which.
#
# Add one by copying examples/bench-rig.toml in here and editing it, or from
# the studio: Admin, Devices. The conductor reads this directory rather than a
# named file, so it plays whichever rig was picked.
